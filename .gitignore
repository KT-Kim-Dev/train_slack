# 의존성
node_modules/

# 빌드 산출물
dist/
out/
release/
*.tsbuildinfo
# client/build/ 는 electron-builder 리소스(아이콘 등) 포함 — 추적 대상
# 서버/기타 build/ 디렉터리는 제외
server/build/

# 런타임 로그 (별도 보관, 저장소에는 포함하지 않음)
logs/
*.log

# SQLite 데이터베이스 파일
data/
*.sqlite
*.sqlite3
*.db

# 업로드된 파일 (개발용 로컬 저장소)
uploads/

# 환경 변수
.env
.env.local

# 임시 검증 산출물
.tmp/

# OS / 에디터
.DS_Store
Thumbs.db
.vscode/
.idea/
